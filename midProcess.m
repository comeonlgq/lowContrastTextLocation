%This function used the  MSER method to gain some areas ,some of which contains text,some are not. 
% In order to get a perfect result,we also used the vertical projection and horizontal projection.

% img is just the image data through preProcess.m
% outImg is the post process by the funtion midProcess

function outImg = midProcess(img)


end